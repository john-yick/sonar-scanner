resources:
- name: sonar-scanner-source
  type: git
  source: 
    uri: https://github.com/emerald-squad/sonar-scanner.git
    branch: 'master'

- name: docker-hub
  type: docker-image
  source:
    repository: emeraldsquad/sonar-scanner-test
    username: ((docker-hub-username))
    password: ((docker-hub-password))

jobs:
- name: publish
  plan:
  - aggregate:
    - get: sonar-scanner-source
  - put: docker-hub
    params:
      build: sonar-scanner-source
      tag: sonar-scanner-source/version
      tag_as_latest: false

- name: tag-as-latest
  plan:
  - aggregate:
    - get: sonar-scanner-source
      passed: [ publish ]
    - get: docker-hub
      params:
        tag: sonar-scanner-source/version
        save: true
  - put: docker-hub
    params:
      load: docker-hub

